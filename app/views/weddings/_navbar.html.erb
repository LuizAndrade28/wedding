<ul class="nav justify-content-center align-items-center nav-wed gap-2" data-controller="navbar-wed" data-navbar-wed-class-value="navwed-height-size">
  <li class="nav-item text-decoration-none">
    <a class="nav-link active text-decoration-none">Confirme sua presença</a>
  </li>
  <li class="nav-item text-decoration-none">
    <a class="nav-link text-decoration-none">Recepção</a>
  </li>
  <% if !@gifts.empty? && !@gifts.nil? %>
    <li class="nav-item text-decoration-none">
      <%= link_to "Lista de presentes", presentes_path(), class: "nav-link text-decoration-none" %>
    </li>
  <% end %>
  <% if !@wedding_guests_messages.nil? && !@wedding_guests_messages.empty? %>
    <li class="nav-item text-decoration-none">
      <a class="nav-link text-decoration-none">Mensagens</a>
    </li>
  <% end %>
  <% if !@tips.nil? && !@tips.empty? %>
    <li class="nav-item text-decoration-none">
      <a class="nav-link text-decoration-none">Dicas</a>
    </li>
  <% end %>
</ul>
